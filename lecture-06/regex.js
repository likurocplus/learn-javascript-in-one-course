// regex1 and regex2 are Regular Expression in Javascript
let regex1 = /luong*.+/g
let regex2 = new RegExp('luong*.+', 'g') // we will talk more about this syntax later
console.log('regex1:', regex1) // regex1: /luong*.+/g
console.log('regex2:', regex2) // regex2: /luong*.+/g